/**
 * @stride/shared-types — the API contract shared by the backend, the web
 * client, and (later) the generated iOS client. Zod schemas are the source of
 * truth; the OpenAPI spec is emitted from them.
 */
export * from "./errors";
